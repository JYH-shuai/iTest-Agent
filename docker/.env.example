# iTest-Agent 环境变量示例（复制为 .env 使用）

# LLM 配置（留空则启用规则解析降级）
OPENAI_API_KEY=
OPENAI_BASE_URL=

# 默认以 Mock LLM + 模拟执行模式启动，便于开箱演示
ITEST_MOCK_LLM=1
ITEST_EXECUTION_MODE=simulated

# 接口测试默认 Base URL（MCP 真实执行时使用）
ITEST_API_BASE_URL=

# 执行超时（秒）
ITEST_EXECUTION_TIMEOUT=30
